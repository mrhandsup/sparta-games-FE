import { AiFillCloseCircle } from "react-icons/ai";

const CloseCircle = ({ size, color }: { size: number; color: string }) => {
  return <AiFillCloseCircle size={size} color={color} />;
};

export default CloseCircle;
